const MapView = () => (
  <>
    <p>No map for iOS just yet...</p>
  </>
);

const Marker = () => (
  <>
    <p>No Marker for iOS just yet...</p>
  </>
);

export default MapView;

export { Marker };
