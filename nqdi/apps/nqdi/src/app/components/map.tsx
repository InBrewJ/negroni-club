const MapView = () => (
  <>
    <p>This is the fallback MapView</p>
  </>
);

const Marker = () => (
  <>
    <p>This is the fallback Marker</p>
  </>
);

export default MapView;

export { Marker };
