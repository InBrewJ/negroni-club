const MapView = () => (
  <>
    <p>No map for Android just yet...</p>
  </>
);

const Marker = () => (
  <>
    <p>No Marker for Android just yet...</p>
  </>
);

export default MapView;

export { Marker };
